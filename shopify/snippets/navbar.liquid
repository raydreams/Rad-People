<nav class="navbar-container">
  <div class="desktop-nav">
    <a href="{{ routes.root_url }}" class="navbar-logo">RADPEOPLE</a>

    <div class="navbar-links">
      <a href="{{ pages.about.url }}" class="navbar-link {% if request.page_type == 'page' and page.handle == 'about' %}active{% endif %}">
        ABOUT
      </a>
      <a href="{{ pages.gallery.url }}" class="navbar-link {% if request.page_type == 'page' and page.handle == 'gallery' %}active{% endif %}">
        GALLERY
      </a>
      <a href="{{ routes.collections_url }}" class="navbar-link {% if request.page_type == 'collection' or request.page_type == 'product' %}active{% endif %}">
        SHOP
      </a>
      <a href="{{ pages.events.url }}" class="navbar-link {% if request.page_type == 'page' and page.handle == 'events' %}active{% endif %}">
        EVENTS
      </a>
      <a href="{{ pages.clients.url }}" class="navbar-link {% if request.page_type == 'page' and page.handle == 'clients' %}active{% endif %}">
        CLIENTS
      </a>
    </div>

    <button type="button" class="cart-link" onclick="if (typeof openCartDrawer === 'function') openCartDrawer();">
      CART
      <span class="cart-indicator {% if cart.item_count > 0 %}pulse{% endif %}" {% unless cart.item_count > 0 %}style="display: none;"{% endunless %}></span>
    </button>
  </div>

  <div class="mobile-nav">
    <button type="button" class="menu-icon" onclick="toggleMobileMenu()">
      <span></span>
      <span></span>
      <span></span>
    </button>

    <a href="{{ routes.root_url }}" class="mobile-logo">RADPEOPLE</a>

    <button type="button" class="cart-icon" onclick="if (typeof openCartDrawer === 'function') openCartDrawer();">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1">
        <path d="M6 2L3 6v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6l-3-4z"></path>
        <line x1="3" y1="6" x2="21" y2="6"></line>
        <path d="M16 10a4 4 0 0 1-8 0"></path>
      </svg>
      <span class="cart-icon-indicator {% if cart.item_count > 0 %}pulse{% endif %}" {% unless cart.item_count > 0 %}style="display: none;"{% endunless %}></span>
    </button>
  </div>

  <div class="mobile-menu" id="mobileMenu">
    <div class="mobile-menu-links">
      <a href="{{ pages.about.url }}" class="mobile-menu-link">
        <span>ABOUT</span>
        <span class="mobile-menu-icon">→</span>
      </a>
      <a href="{{ pages.gallery.url }}" class="mobile-menu-link">
        <span>GALLERY</span>
        <span class="mobile-menu-icon">→</span>
      </a>
      <a href="{{ routes.collections_url }}" class="mobile-menu-link">
        <span>SHOP</span>
        <span class="mobile-menu-icon">→</span>
      </a>
      <a href="{{ pages.events.url }}" class="mobile-menu-link">
        <span>EVENTS</span>
        <span class="mobile-menu-icon">→</span>
      </a>
      <a href="{{ pages.clients.url }}" class="mobile-menu-link">
        <span>CLIENTS</span>
        <span class="mobile-menu-icon">→</span>
      </a>
    </div>
  </div>
</nav>

<script>
  function toggleMobileMenu() {
    const menu = document.getElementById('mobileMenu');
    menu.classList.toggle('open');
  }
</script>

