{% comment %}
  Clients Page Component
  Note: This will need to be populated with Shopify metafields
{% endcomment %}

<div class="clients-container">
  <div class="clients-list">
    {% comment %} Clients can be added via metafields or a custom section {% endcomment %}
    <p style="font-family: 'Sequel Sans Regular', sans-serif; color: black; padding: 20px;">
      Client logos and information will be displayed here. Use Shopify metafields to add clients.
    </p>
  </div>
</div>

<style>
  .clients-container {
    padding: 40px 20px;
    min-height: calc(100vh - 40px);
    background-color: white;
  }

  .clients-list {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 40px;
    align-items: center;
  }

  .client-item {
    text-align: center;
  }

  .client-item img {
    max-width: 100%;
    height: auto;
    filter: grayscale(100%);
    transition: filter 0.3s ease;
  }

  .client-item img:hover {
    filter: grayscale(0%);
  }

  @media (max-width: 768px) {
    .clients-list {
      grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
      gap: 20px;
    }
  }
</style>

