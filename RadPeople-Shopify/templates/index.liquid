{% comment %}
  Homepage template
  Note: Video URLs can be updated after hosting videos externally (Google Drive, CDN, etc.)
  For trial accounts, use external hosting since Shopify doesn't support video uploads
{% endcomment %}

{% render 'intro-animation' %}

<div class="home-container">
  <div class="video-wrapper">
    <video class="styled-video" autoplay loop muted playsinline>
      {% comment %} 
        TODO: Replace these URLs with your hosted video URLs
        Option 1: Google Drive direct links (https://drive.google.com/uc?export=download&id=FILE_ID)
        Option 2: CDN URLs (AWS S3, Cloudflare R2, etc.)
        Option 3: Remove video section and use static background (see comments below)
      {% endcomment %}
      <source src="{{ 'radpeople-landingPage.mp4' | asset_url }}" type="video/mp4" media="(min-width: 768px)">
      <source src="{{ 'landingVideo-mobile.mp4' | asset_url }}" type="video/mp4" media="(max-width: 767px)">
      Your browser does not support the video tag.
    </video>
  </div>
</div>

{% comment %}
  ALTERNATIVE: If videos aren't working, replace the video section above with this:
  
  <div class="home-container" style="min-height: 100vh; background-color: #1404FB; display: flex; align-items: center; justify-content: center;">
    <h1 style="font-family: 'NF Ultra', sans-serif; color: white; font-size: 4rem; text-align: center;">RADPEOPLE</h1>
  </div>
{% endcomment %}

{{ 'home.css' | asset_url | stylesheet_tag }}
